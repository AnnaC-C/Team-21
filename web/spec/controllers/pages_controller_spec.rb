require 'rails_helper'

RSpec.describe PagesController, type: :controller do

  describe "GET the dashboard" do
    it "should not GET if not logged in" do
      fail
    end

    it "should GET if logged in" do
      fail
    end
  end
end
